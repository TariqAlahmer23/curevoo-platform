# اختبارات المتطلبات غير الوظيفية

**النظام:** منصة CureVoo — خدمة ترتيب الأهداف الجينومية
**تاريخ القياس:** 19 آب 2026
**المنهجية:** كل رقم هو **الوسيط (median)** لعشر محاولات متتالية، وثلاث إلى خمس محاولات للعمليات الثقيلة. استُخدم الوسيط بدل المتوسط الحسابي حتى لا يشوّه تعثّر واحد في الشبكة النتيجة.

جميع الأرقام في هذا الملف مقيسة فعلياً، ولا يوجد أي رقم مُقدَّر أو مفترض.

---

## 1. متطلب سرعة الأداء والاستجابة

### الجدول رقم 1 — زمن استجابة واجهات البرمجة على بيئتين

قياس مباشر لخدمة الذكاء الاصطناعي (FastAPI) بعد تسخين الخدمة:

| Request | Response Time Locally | Response Time on Host (Render) | النسبة |
| --- | ---: | ---: | ---: |
| GET /health | 22.6 ms | 849.4 ms | 37.6× |
| GET /metrics | 19.1 ms | 755.1 ms | 39.5× |
| POST /analyze (المجموعة المرجعية) | 182.8 ms | 2141.2 ms | 11.7× |
| GET /results/{id} | 15.6 ms | 717.3 ms | 46.0× |
| GET /reports/{id} | 12.4 ms | 746.9 ms | 60.2× |
| POST /analyze (ملفات مرفوعة) | 486.9 ms | 6408.9 ms | 13.2× |

قياس عبر الخادم الخلفي (Node/Express) الذي يتوسط بين الواجهة وخدمة الذكاء الاصطناعي:

| Request | Response Time Locally | Response Time on Host (Render) | النسبة |
| --- | ---: | ---: | ---: |
| GET /health | 19.1 ms | 761.3 ms | 39.9× |
| GET AI health proxy | 18.9 ms | 801.8 ms | 42.4× |
| POST /analyze (المجموعة المرجعية) | 203.6 ms | 2074.3 ms | 10.2× |
| POST /analyze (ملفات مرفوعة) | 509.7 ms | 6814.5 ms | 13.4× |
| POST /api/auth/login | 312.9 ms | 2229.2 ms | 7.1× |

**تحليل النتائج:**

1. الفارق بين البيئتين ليس فارقاً في كفاءة الشيفرة، بل في **زمن انتقال الشبكة**. الخدمة مستضافة في فرانكفورت والقياس من عمّان، ويظهر ذلك بوضوح في المسارات الخفيفة: `GET /reports/{id}` يستغرق 12.4 مللي ثانية محلياً و746.9 مللي ثانية عن بُعد، رغم أن العمل الحسابي متطابق. الفارق البالغ نحو 730 مللي ثانية هو تكلفة الرحلة الشبكية وليس المعالجة.

2. كلما زاد العمل الحسابي، **قلّت نسبة الفارق**. المسارات الخفيفة تُظهر فارقاً يصل إلى 60 ضعفاً، بينما `POST /analyze` مع ملفات مرفوعة يُظهر 13 ضعفاً فقط، لأن زمن المعالجة الفعلي يبدأ في الهيمنة على زمن الشبكة.

3. **تكلفة الوساطة عبر الخادم الخلفي منخفضة.** الفرق بين استدعاء خدمة الذكاء الاصطناعي مباشرة (182.8 مللي ثانية) واستدعائها عبر الخادم الخلفي (203.6 مللي ثانية) هو 20.8 مللي ثانية فقط محلياً، أي أن الطبقة الوسيطة تضيف نحو 11% فقط مقابل ما توفّره من توثيق وتحقق من المدخلات وإخفاء للأخطاء الداخلية.

### الجدول رقم 2 — أثر التخزين المؤقت في الذاكرة

تعتمد الخدمة على دالة `_read_cached_csv` التي تحتفظ بالجداول المُحلَّلة في الذاكرة وتربطها بمسار الملف وتاريخ تعديله. أول طلب بعد إقلاع الخدمة يدفع كلفة تحليل الملفات، وما بعده يقرأ من الذاكرة.

القياس تم بعد إعادة تشغيل الخدمة فعلياً لضمان أن ذاكرة التخزين فارغة:

| Request | أول طلب (الذاكرة فارغة) | الوسيط بعد التخزين | التحسن |
| --- | ---: | ---: | ---: |
| POST /analyze (المجموعة المرجعية) | 260.2 ms | 194.3 ms | 1.3× |
| GET /metrics | 8.4 ms | 18.0 ms | لا يوجد فرق يُعتد به |

**قراءة صادقة للنتيجة:**

التحسن الفعلي **متواضع (1.3×) وليس كبيراً**، ويجب عدم المبالغة في وصفه. السبب أن نظام التشغيل يحتفظ بنسخة من الملفات في ذاكرة القرص المؤقتة، فحتى القراءة «الباردة» لا تصل إلى القرص الفعلي في أغلب الحالات. ولذلك فإن ما توفّره الذاكرة هو كلفة **تحليل** الملف بواسطة pandas وليس كلفة قراءته من القرص.

أما `GET /metrics` فلم يُظهر تحسناً، بل ظهر أبطأ قليلاً بعد التخزين، وهذا ضجيج قياس طبيعي لأن الملفين اللذين يقرأهما لا يتجاوزان 3 كيلوبايت مجتمعين، فلا مجال لتحسين يُذكر أصلاً.

حجم البيانات المُحمَّلة في الذاكرة عند التشغيل الكامل:

| الملف | الحجم |
| --- | ---: |
| target_ranking_v6_evidence_tiers.csv | 10.85 MB |
| gtex_lung_safety_features.csv | 13.06 MB |
| external_targets_consensus_ncg_civic.csv | 0.88 MB |
| **الإجمالي** | **24.79 MB** |

### الجدول رقم 3 — زمن الإقلاع البارد على الاستضافة المجانية

خطة Render المجانية تُدخل الخدمة في وضع السكون بعد نحو 15 دقيقة من الخمول:

| الخدمة | زمن أول طلب بعد السكون | زمن الطلب بعد التسخين |
| --- | ---: | ---: |
| خدمة الذكاء الاصطناعي (FastAPI) | 22.62 s | 0.85 s |
| الخادم الخلفي (Node) | 32.89 s | 0.76 s |
| الواجهة (ملفات ثابتة) | 0.62 s | 0.62 s |

الواجهة لا تتأثر بالسكون لأنها ملفات ثابتة تُخدَّم من شبكة توزيع المحتوى ولا تحتاج عملية قيد التشغيل.

لهذا السبب تحديداً ضُبط المتغير `AI_GENOMIC_HEALTH_TIMEOUT_MS` على 60000 مللي ثانية بدل القيمة الافتراضية 8000، لأن فحص الجاهزية كان يفشل أثناء صحو الخدمة ويُرجع الخطأ `AI_SERVICE_UNAVAILABLE` رغم سلامة النظام.

---

## 2. متطلب زمن تحميل الواجهة

### الجدول رقم 4 — أثر ضغط النقل على حجم الحزمة

الاستضافة تُطبّق ضغط Brotli تلقائياً على الملفات النصية:

| Metric | Without Compression | With Brotli Compression | التحسن |
| --- | ---: | ---: | ---: |
| main.dart.js | 4347.7 KB | 1218.6 KB | 3.57× |
| MaterialIcons-Regular.otf | 26.8 KB | 14.4 KB | 1.86× |
| flutter_bootstrap.js | 9.7 KB | 3.7 KB | 2.62× |
| index.html | 1.2 KB | 0.6 KB | 2.00× |
| curvoo.png (الشعار) | 1222.4 KB | 1222.4 KB | **1.00× — لا ضغط** |

### الجدول رقم 5 — زمن التحميل: أول زيارة مقابل زيارة متكررة

| Metric | First Visit (بدون cache) | Repeat Visit (مع cache) |
| --- | ---: | ---: |
| إجمالي البيانات المنقولة | 2459.7 KB | 2.1 KB |
| زمن تحميل main.dart.js | 3139 ms | من الذاكرة المؤقتة |
| زمن تحميل الشعار | 2844 ms | من الذاكرة المؤقتة |
| Time To First Byte | 290 ms | 290 ms |
| DOMContentLoaded | — | 512 ms |
| Load Event | — | 642 ms |
| استهلاك الذاكرة (JS Heap) | — | 39.7 MB |
| عدد الموارد المُحمَّلة | 10 | 10 |

---

## 3. نتيجة مهمة: الشعار يمثل نصف حجم التحميل

القياس كشف مشكلة أداء حقيقية لم تكن ظاهرة قبل القياس.

ملف `curvoo.png` حجمه **1222.4 كيلوبايت**، ويُنقل **دون أي ضغط** لأنه صورة نقطية مضغوطة أصلاً بصيغتها. وهذا يعني أن الشعار وحده يعادل تقريباً حجم حزمة التطبيق كاملة بعد ضغطها (1218.6 كيلوبايت)، أي أنه يشكّل نحو **50% من إجمالي البيانات المنقولة في أول زيارة**.

والمفارقة أن هذا الشعار يُعرض في الواجهة بأبعاد لا تتجاوز **54 بكسل** في الشريط الجانبي.

| البند | القيمة |
| --- | ---: |
| الحجم الحالي | 1222.4 KB |
| أكبر أبعاد عرض فعلية | 54 × 54 بكسل |
| نصيبه من تحميل أول زيارة | نحو 50% |
| زمن تحميله المقيس | 2844 ms |

**التوصية:** إعادة حفظ الشعار بأبعاد مناسبة للعرض (مثلاً 128×128 بكسل) وبصيغة WebP أو PNG مُحسَّن. الحجم المتوقع بعد ذلك أقل من 20 كيلوبايت، أي خفض يقارب **98%** من حجم الملف، وخفض إجمالي التحميل في أول زيارة من 2459.7 كيلوبايت إلى نحو 1257 كيلوبايت.

هذا التحسين **لم يُنفَّذ** ضمن هذه الجولة لأنه يتطلب تعديل أصل من أصول المشروع، وهو متروك لقرار المطوّر.

---

## 4. ملخص المتطلبات غير الوظيفية

| المتطلب | المعيار المستهدف | النتيجة المقيسة | الحالة |
| --- | --- | --- | --- |
| زمن استجابة العمليات الخفيفة محلياً | أقل من 100 مللي ثانية | 12.4 – 22.6 مللي ثانية | مستوفى |
| زمن استجابة التحليل محلياً | أقل من ثانية واحدة | 182.8 مللي ثانية للمجموعة المرجعية و486.9 مللي ثانية للملفات المرفوعة | مستوفى |
| زمن استجابة التحليل على الاستضافة | أقل من 10 ثوانٍ | 2.14 ثانية للمجموعة المرجعية و6.41 ثانية للملفات المرفوعة | مستوفى |
| زمن تحميل الواجهة في زيارة متكررة | أقل من ثانيتين | 642 مللي ثانية | مستوفى |
| زمن تحميل الواجهة في أول زيارة | أقل من 5 ثوانٍ | نحو 3.1 ثانية للحزمة الرئيسية | مستوفى |
| استهلاك الذاكرة في المتصفح | أقل من 100 ميغابايت | 39.7 ميغابايت | مستوفى |
| زمن الإقلاع البارد | غير محدد في المتطلبات | 22.6 – 32.9 ثانية | قيد الملاحظة — قيد الخطة المجانية |
| حجم التحميل الأولي | أقل من 3 ميغابايت | 2459.7 كيلوبايت | مستوفى مع وجود فرصة تحسين واضحة في الشعار |

---

## 5. حدود القياس

تُذكر صراحةً حتى لا يُقرأ هذا التقرير على أنه يثبت أكثر مما قِيس فعلاً:

1. **لم يُجرَ اختبار حِمل أو تزامن.** جميع القياسات لطلب واحد في كل مرة. سلوك النظام تحت عشرات الطلبات المتزامنة غير معروف.

2. **القياس من موقع جغرافي واحد.** جميع قياسات الاستضافة من عمّان إلى فرانكفورت. النتائج ستختلف من مواقع أخرى.

3. **الشبكة غير مضبوطة.** لم تُستخدم بيئة شبكة معزولة، لذا قد يتضمن الفارق بين البيئتين تذبذباً في اتصال الإنترنت المستخدم. استُخدم الوسيط للتخفيف من ذلك لكنه لا يلغيه.

4. **قياس «الذاكرة الباردة» متأثر بذاكرة نظام التشغيل.** كما شُرح في الجدول رقم 2، القراءة الباردة لا تصل إلى القرص فعلياً في أغلب الحالات، لذا فإن 1.3× تمثل الحد الأدنى للتحسن وليس أقصاه.

5. **قياسات المتصفح للزيارة المتكررة فقط.** لم يُتَح مسح ذاكرة المتصفح المؤقتة برمجياً، لذا أرقام أول زيارة مأخوذة من قياس شبكي مستقل يتجاوز ذاكرة المتصفح، لا من واجهة قياس الأداء في المتصفح.

6. **لا يوجد Redis ولا React في هذا النظام.** آلية التسريع الموجودة فعلياً هي تخزين مؤقت للجداول في ذاكرة العملية داخل خدمة FastAPI، وقد قيست كما هي دون محاولة مقارنتها بتقنيات غير مستخدمة في المشروع.

---

## 6. أوامر إعادة التنفيذ

قياس زمن الاستجابة محلياً:

```bash
python benchmark.py --target ai --base http://127.0.0.1:8001 --runs 10 --heavy-runs 5
```

قياس زمن الاستجابة على الاستضافة:

```bash
python benchmark.py --target ai --base https://curevoo-ai-service.onrender.com --runs 10 --heavy-runs 3 --warmup
```

قياس أثر التخزين المؤقت — يجب إعادة تشغيل الخدمة أولاً لتفريغ الذاكرة:

```bash
cd dip-ai-service && python -m uvicorn app.api.app:app --host 127.0.0.1 --port 8001
```

قياس زمن الإقلاع البارد بعد فترة خمول تتجاوز 15 دقيقة:

```bash
curl -s -o /dev/null -w "%{time_total}s\n" https://curevoo-ai-service.onrender.com/health
```
